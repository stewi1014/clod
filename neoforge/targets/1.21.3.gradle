plugins {
    id 'java'
    id 'net.neoforged.moddev' version '1.0.21'
}

neoForge {
    version = '21.3.66'

    runs {
        client { client() }
        server { server() }
    }
}

dependencies {
    implementation(project(':mod'))
}
